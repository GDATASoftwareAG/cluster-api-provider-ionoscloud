apiVersion: cluster.x-k8s.io/v1beta1
kind: MachineDeployment
metadata:
  labels:
    cluster.x-k8s.io/cluster-name: ionoscloud-local-matthias
  name: ionoscloudcluster-md-0
  namespace: default
spec:
  clusterName: ionoscloud-local-matthias
  replicas: 2
  selector:
    matchLabels: {}
  template:
    metadata:
      labels:
        cluster.x-k8s.io/cluster-name: ionoscloud-local-matthias
    spec:
      bootstrap:
        configRef:
          apiVersion: bootstrap.cluster.x-k8s.io/v1beta1
          kind: KubeadmConfigTemplate
          name: ionoscloudcluster-md-0
      clusterName: ionoscloud-local-matthias
      infrastructureRef:
        apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
        kind: IONOSCloudMachineTemplate
        name: ionoscloud-local-matthias-worker
      version: "1.26.7"
